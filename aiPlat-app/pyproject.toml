[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aiplat-app"
version = "0.1.0"
description = "AI Platform App Layer - Message Gateway, CLI, Workbench"
requires-python = ">=3.10"
dependencies = [
    "aiplat-platform",
    "click>=8.0",
    "requests>=2.28",
]
