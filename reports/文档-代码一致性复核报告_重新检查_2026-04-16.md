# 文档-代码一致性复核报告（重新检查）

日期：2026-04-16  
范围：仓库全部 `*.md` 文档与仓库内可见代码（`aiPlat-core/core`、`aiPlat-infra/infra`、`aiPlat-management/management`）

> 本报告基于“自动化静态检查 + 关键断言抽样核对”。  
> 自动化检查可以高置信发现：断链、明显错误的内部 import 示例、以及“文档宣称存在但仓库内无代码承载”的结构风险。

---

## 1. 关键结论（TL;DR）

1) **文档导航一致性已修复（P0 清零）**：失效相对链接 **0**（146 份 Markdown 中）。  
2) **示例代码 import 一致性已修复（P1 清零）**：疑似失效的“内部模块”Python import 示例 **0**。  
3) **层级实现可验证性不均衡（结构性风险）**：  
   - ✅ `aiPlat-core / aiPlat-infra / aiPlat-management`：仓库内有真实代码与测试，可被验证。  
   - ⚠️ `aiPlat-platform / aiPlat-app`：仓库内仅存在 docs（未发现对应源码包/服务实现），因此文档中“导入示例/SDK/可运行服务”的断言无法在本仓库自证。

---

## 2. 自动化静态检查指标

检查脚本：`work/doc_audit.py`（best-effort）

- 扫描 Markdown 文件数：146
- 失效相对链接：0
- 疑似失效的内部 Python import 示例：0

---

## 3. 问题明细（摘要）

### 3.1 P0：失效相对链接（已修复，当前为 0）

最常见类型是**跨层链接层级写错**（从 `docs/` 或 `aiPlat-app/docs` 指向 `../aiPlat-xxx/...` 时少退了一层，导致解析到 `docs/aiPlat-xxx/...` 或 `aiPlat-app/aiPlat-xxx/...`）。

上述问题已通过批量修正相对路径层级与跨层引用规则解决（见后续“修复建议”落地情况）。

### 3.2 P1：内部 import 示例不匹配（已修复，当前为 0）

历史上主要集中在以下几类（现均已修正为真实可导入路径或明确标注为伪代码）：

1) **platform/app 包不存在（结构性）**
- `from aiPlat_platform import APIRouter / APIClient`
- `from aiplat_platform import PlatformClient`
- `from aiPlat_app.events import ...`

2) **core 文档示例引用不存在的子模块**
- `docs/tools/enhancement.md` 引用：`core.apps.tools.hooks...`（仓库中 `aiPlat-core/core/apps/tools` 下未发现 `hooks` 子模块）
- `aiPlat-core/docs/testing/TESTING_GUIDE.md` 引用：`from harness.config import HarnessConfig`（`aiPlat-core/core/harness` 下无 `config.py`）

3) **infra 文档示例引用不存在的子模块**
- `infra.database.oracle`
- `infra.config.sources.consul`
- `infra.network.discovery.*`
- `infra.mcp.config`
- `infra.observability.exporters.*`
- `infra.monitoring.health`
- `infra.logging.outputs.base` / `infra.logging.formatter`

---

## 4. 关键断言抽样核对（Spot-check）

### 4.1 Observability exporter：Zipkin/Jaeger
- 在 `aiPlat-infra/infra/observability/otel.py` 中仅看到 `otlp/grpc` 分支与 `OTLPSpanExporter`，未发现 `zipkin/jaeger` 关键词或分支。
- 结论：若文档宣称“Zipkin/Jaeger 已支持”，属于 **文档过誉** 或 **实现未纳入仓库**。

### 4.2 DI：作用域用法
- `aiPlat-infra/infra/di/container.py` 中存在 `container.scope(...)` 方法；这一点与我们已修复后的文档示例一致。

---

## 5. 建议修复顺序

1) **先清零 P0 断链（最影响文档可用性）**
   - 优先修 `docs/`（系统级入口）与 `aiPlat-app/docs/index.md` 的跨层链接。
   - 建议把 link-check 加入 CI，避免回归。

2) **将示例代码“分级”**
   - 可运行示例：必须能在本仓库 import 成功（否则不得写成可运行）
   - 伪代码：明确标注“伪代码”，不要写真实 import 路径

3) **明确 platform/app 的仓库现状**
   - 要么补齐源码纳入仓库；要么在 docs 顶部明确声明“仅设计/契约，代码不在本仓库/未落地”，并移除导入示例。
