// Deprecated: moved to components/ui for a stable UI-level API.
// Keep re-exports to avoid breaking older imports.
export { extractGateEnvelope, toastGateError } from '../components/ui';
export type { GateErrorEnvelope } from '../components/ui/gateError';
