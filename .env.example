# ============================================================
# aiPlat 环境配置 — 客户部署模板
# 复制为 .env 后填入实际值
# ============================================================

# --- Core AI Engine (port 8002) ---
AIPLAT_SKILL_STRICT_VALIDATION=false
AIPLAT_RBAC_MODE=warn

# --- LLM API Keys ---
# 不再从环境变量读取。通过 Management UI (模型管理页面) 或 SQLite adapters 表管理。
# SQLite: ~/.aiplat/aiplat_executions.sqlite3 → adapters 表
# 新增模型: INSERT INTO adapters (adapter_id,name,provider,status,api_key,...)
# 更新 key:  UPDATE adapters SET api_key='sk-xxx' WHERE adapter_id='...'
# 管理界面: http://localhost:5173/core/models (需启动 server)

# --- Ollama (本地模型) ---
OLLAMA_BASE_URL=http://host.docker.internal:11434
# 若 Ollama 与 Docker 不在同一机器，改为实际 IP

# --- 数据库 ---
AIPLAT_DB_PATH=/data/aiplat

# --- 管理员初始账户（首次启动时创建） ---
AIPLAT_ADMIN_USERNAME=admin
AIPLAT_ADMIN_PASSWORD=changeme

# --- 网络/端口 ---
AIPLAT_PORT_SERVICES="8002:core,8001:infra,8003:platform,8004:app,8000:management,5173:frontend"

# --- 存储路径 ---
AIPLAT_STORAGE_BUCKET=aiplat-data

# --- 监控 ---
AIPLAT_METRICS_NAMESPACE=aiplat-production
AIPLAT_SERVICE_NAME=aiplat-deployment

# --- 通知 (可选) ---
# AIPLAT_SMTP_HOST=smtp.example.com
# AIPLAT_SMTP_PORT=587
# AIPLAT_SMTP_USER=noreply@example.com
# AIPLAT_SMTP_PASS=password

# ════════════════════════════════════════════════════════════════
# Opt-in 能力开关（P0-B5 清单化 — 全部默认关闭，显式开启才生效）
# 这些能力已实现且已接线（flag 只是运行时开关，非未接线占位）。
# 默认部署只生效 core 链路；以下能力需显式开启：
# ════════════════════════════════════════════════════════════════

# RL 训练循环（重能力，默认关）
# AIPLAT_RL_ENABLED=false
# RL 在线模式（需 RL_ENABLED=true 才有意义）
# AIPLAT_RL_ONLINE=false

# 学习指标快照 + 自动回滚循环（LearningManager 后台调度）
# AIPLAT_ENABLE_LEARNING_SCHEDULER=false
# 回归自动回滚
# AIPLAT_ENABLE_LEARNING_AUTOROLLBACK=false
# 学习产物应用（3 处引用点均需开启）
# AIPLAT_ENABLE_LEARNING_APPLIER=false

# release 灰度发布
# AIPLAT_ENABLE_RELEASE_ROLLOUTS=false

# 会话检索上下文注入
# AIPLAT_ENABLE_SESSION_SEARCH=false

# toolsets 集成
# AIPLAT_ENABLE_TOOLSETS=false

# OTel 遥测导出
# AIPLAT_OTEL_ENABLED=false
