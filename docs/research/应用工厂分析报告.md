# aiPlat 应用工厂（AI App Factory）分析报告（2026-08-22）

> **最后验证：2026-08-26**（L3 增量合并引擎实施后复核：merge_engine 已登记 capability_registry；代码证据见附录 A 及 `plan-app-factory-l3-incremental-engine.md`；2026-08-25 复核：事件源纯度 fork 会话实施（pipeline_run_store/pipeline_execution）不影响本报告应用工厂结论；**2026-08-25 §7.5 改为实现代码审计**：5 组 26 文件深度分析（workflow 5 子代理 + 人工抽查 8 项复核），发现 5 P0 + 23 P1 + 19 P2，综合 2.9/5——见 §7.5）

> 一句话：**应用工厂 = "AI 帮你做软件"的自动化产线**——你描述想要什么，它自动组一支"AI 团队"把东西做出来、测好、部署上线。像点菜：你说想吃啥，后厨（AI 团队）配菜、炒菜、试菜、上桌，你只管验收。

---

## 1. 它解决什么问题？

传统做软件：需求→设计→编码→测试→上线，要多个角色、数周时间。应用工厂把这条链**压缩成对话**：

| 传统方式 | 应用工厂 |
|---|---|
| 写需求文档 | 和 AI 对话，AI 自动整理成 PRD |
| 找开发/前端/测试 | AI 自动组队（按历史表现挑 Agent） |
| 写代码 | AI 生成（要么生成"Agent 配置"，要么生成真实代码） |
| 人工测试 | AI 跑测试并报告通过率 |
| 部署上线 | AI 部署到平台，你直接预览 |

**适用对象**：不懂代码的业务用户（一句话建应用）+ 懂技术的用户（精确控制生成过程）。

## 2. 入口与三种用法

入口在侧边栏 **"🤖 AI 应用工厂"**（唯一入口，旧的 /studio、/workbench 会自动跳过来）。进去后有三个 Tab，按你的熟悉程度选：

| 用法 | 适合谁 | 怎么用 |
|---|---|---|
| **快速开始** | 想直接上手的用户 | 选一个能力（如"数据录入""问答"）→ 填一句描述 → 一键开始，页面实时显示进度 |
| **对话式** | 需求还没想清楚 | 直接和 AI 聊天，它会追问澄清（"要几个人审批？""数据从哪来？"），聊完自动组队开工 |
| **高级配置** | 技术用户 | 能看到全部项目、手动调 Agent 组合/阶段/审批点，随时介入 |

## 3. 关键机制：AI 自己判断"该怎么做"（双模式）

这是应用工厂最聪明的地方——**AI 看了你的需求后，自动决定走哪条生产线**：

```
你的需求（如"做一个客服问答机器人"）
        ↓  AI 判断
   ┌────┴────┐
 对话型       计算型
（聊天/问答）  （查数据/算结果/表单）
   ↓           ↓
【Agent 模式】  【代码模式】
生成"AI 助手配置"  生成"真实程序代码"
（给平台配一个会   （像程序员写代码一样，
 对话的助手）      产出可运行的文件+测试）
```

- **Agent 模式**（默认，适合聊天机器人、问答助手这类）：产出的是"平台配置"——告诉平台"这里要一个会回答问题的助手"，由平台渲染成可交互页面。快，但不产生独立代码。
- **代码模式**（适合数据处理、CRUD、上传转码这类）：产出的是**真正的源代码文件**，会落盘保存，并**真的跑测试**（pytest）验证。

判断标准一句话：**"这东西的核心是'和人聊天'还是'算东西/存数据'？"**——前者走 Agent，后者走代码。

## 4. 从需求到上线：你看到的全过程

```
第1步 对话澄清   你和 AI 聊需求（它会参考公司知识库，更懂你）
第2步 确认 PRD   AI 把聊天的结果整理成《需求说明书》，你确认
第3步 自动组队   AI 按需求类型 + 各 Agent 的历史表现，组一支团队
第4步 团队干活   PM→架构师→开发→前端→测试 依次执行（关键节点暂停等你审批）
第5步 你审批     架构方案、测试用例等关键环节：通过/打回重做
第6步 自动修复   测试失败 → AI 自动找问题、改、重测
第7步 部署上线   通过后部署，给你一个链接直接打开看效果
第8步 随时改     「重新编辑需求」或回滚到任意阶段，重来
—— 改造老项目时（L2-L5 演进链路，见 §9）——
第9步 导入既有代码  上传 zip/路径 → 文件清单 + 修改意图
第10步 增量合并审批  选择"重写"或"增量合并"→ 逐文件 diff 审批（原子化）
第11步 数据库迁移    模型变更 → up/down DDL 预览 → 破坏性确认 → 应用
第12步 受控发布      版本化产物 → 金丝雀（权重）→ 全量/回滚
```

> **审批点设计**：不是全自动黑箱——架构、测试用例这些"关键决策"会停下来等你点头（HITL 机制）。你可以完全放手，也可以全程盯。

## 5. 和公司其他能力怎么配合

- **知识库**：AI 和你对话时，会检索公司已有资料当参考（不是凭空猜）
- **Agent 体系**：组队时参考每个 Agent 过去"一次通过率、被打回率"，挑靠谱的
- **数字人"小朱"**：在应用工厂页面，可以直接问小朱"有几个项目在跑？平均通过率多少？"
- **部署平台**：成品部署到统一的应用区（端口 8004），和平台其他应用一起管理

## 6. 权限：谁能用

| 操作 | 权限 |
|---|---|
| 创建项目/团队 | **管理员**（admin） |
| 对话/审批/查看/部署 | **Builder 角色**（普通参与即可） |

## 7. 诚实地说：5 个要知道的边界

1. **"通过率"有两种来源**——真跑过测试的（可信）和 AI 估算的（仅参考）。现在的版本会明确标注：`real_pytest`（真测试）或 `estimated`（估算）。看到 100% 先看是不是估算的。
2. **前端页面是"搭积木"——但积木已扩展（2026-08-25）**——agent 模式页面由预置组件拼装（`app_page.json` 声明式），不再支持随意自定义界面（那是 code 模式/前端开发的活）。**2026-08-25 解决"组件太少 + 分发硬编码"**：组件注册表（`AppStages/index.ts` `APP_STAGE_REGISTRY`）替代 AppPage 硬编码 if 链，新增组件无需改 AppPage；组件从 5 种扩到 8 种（+markdown_viewer 文本展示 / stat_cards KPI 卡片 / kanban_board 看板，chat_panel 由侧边栏承担），data_form 字段类型补 select/date/number/textarea——与 frontend_developer AGENT.md 组件清单对齐（此前 AGENT.md 声明了 kanban_board/stat_cards/chat_panel 但前端不存在 → 渲染"未知组件"，现已修复）。**剩余边界**：复杂/高度定制界面仍需 code 模式生成真实 React 应用。
3. **测试好坏看 AI 生成的用例**——代码模式真跑 pytest，但测试用例本身是 AI 写的，AI 用例写得松，全绿也不代表真的没问题。
4. **两个入口有点重**——`/app/factory` 是完整入口，但 `/app/builder/projects`（纯列表）也能直达，可能让人疑惑。
5. **已部署应用的可见性**——任何 Builder 角色都能看到全部已部署应用，没有按项目的细粒度隔离。
6. **改既有代码是"重写"或"增量合并"二选一（L2/L3 语义）**——导入后可选"整文件重写（L2）"或"增量合并（L3，推荐）"：L3 只重生成受影响文件、diff 逐文件审批（原子化，全过才应用）、格式噪音自动折叠。金丝雀发布前仍建议跑真实测试（estimated 有提示）。

## 9. 演进链路：从"改单个文件"到"受控发布"（L2→L5，2026-08-22~23 全落地）

应用工厂从"从 0 到 1 生成器"演进为"从 1 到 100 演进器"的完整链路（设计文档：`plan-app-factory-l2-import-repo.md` → `l3-incremental-engine.md` → `l4-multi-module.md` → `l45-db-migration.md` → `l5-cicd.md`）：

| 层 | 能力 | 关键机制 |
|---|---|---|
| **L2 导入既有代码** | zip/路径 → imported/（隔离）→ manifest | zip-slip/密钥过滤/限额 + 意图锚点 {path, intent} + 重写行为契约 + skip_pytest_gate 逃生 |
| **L3 增量合并** | 逐文件 diff 审批（原子化） | 全文件输出 + 系统 difflib 计算；语法 py_compile + 接口 AST 门禁；哈希锁防并发覆盖；格式噪音折叠 |
| **L4 多模块编排** | 模块化系统演进 | modules.json + 跨模块影响（API/entity/事件契约 → 依赖图）+ 拓扑顺序编排 + 跨模块 merge 契约门禁（v1.5） |
| **L4.5 数据库迁移** | 模型变更 → up/down DDL | AST 提取 SQLAlchemy/Pydantic；破坏性（删列/类型变更）需显式确认；迁移历史 append-only + 回滚 |
| **L5 受控发布** | 版本化 + 金丝雀灰度 | releases/v{ts} + 发布状态机（ready→canary→full→rolled_back）+ canary_weight 权重路由（v2）+ infra 注册（CoreFacade 桥接）+ 迁移先行门禁 |

**演进闭环**：AI 改模型 → 增量合并审批（L3）→ 数据库迁移（L4.5）→ 版本化发布（L5）→ 金丝雀验证 → 全量/回滚——代码、schema、发布三者同步受控演进。

**代码证据**：`builder_project_service.py`（import_repo/merge_preview/merge_apply/migration_preview/create_release）、`builder/merge_engine.py`（ImpactAnalyzer/DiffMerger）、`builder/cross_module.py`（CrossModuleAnalyzer）、`builder/schema_migration.py`（SchemaExtractor/MigrationGenerator）、`builder/release_engine.py`（发布状态机）、`core/harness/infrastructure/infra_bridge.py`（deploy_app_service 桥接）、`core/harness/execution/pipeline_engine.py`（inject_imported_context/skip_pytest_gate/UNCHANGED 剔除）、前端 `Factory/index.tsx`（导入面板/合并审批/模块面板/迁移面板/发布区）。

**验证**：L2 34 + L3 25 + L4 21 + L4.5 18 + L5 23 = **121 例测试**全部通过（2026-08-23 复核）。

## 7.5 实现代码分析（2026-08-25 补充——对工厂本身代码的审计）

> 本节不是分析"工厂生成的应用"，而是对**实现应用工厂的代码本身**做深度审计（5 组 26 文件：后端服务 / L2-L5 引擎 / core 引擎侧 / API+前端 / 团队模板）。每条结论附 `文件:行号` 证据，可 grep 复核。**综合评分 2.9/5**（功能完整但存在 5 个 P0 级缺陷需优先修复）。

### 7.5.1 各模块评分与职责

| 模块 | 文件数 | 评分 | 职责 |
|---|---|---|---|
| 后端核心服务 `builder/` | 8 | 2.5 | BuilderProjectService（CRUD/对话/HITL/L2-L5/部署）+ 会话/团队/工作流/应用服务 |
| L2-L5 演进引擎 | 4 | **3.5** | merge_engine / cross_module / schema_migration / release_engine（纯标准库，无 core 直导） |
| core 引擎侧 | 3 | 2.5 | team_planner（双模式路由）+ pipeline_engine code 模式（落盘/测试/上下文注入） |
| API + 前端 | 3 | 3.0 | builder.py router（72 端点）+ Factory/index.tsx（2756 行）+ AppPage.tsx |
| 团队模板 | 8 | 3.0 | default.yaml（agent）/ code.yaml（code）双模式配置 |

### 7.5.2 做得好的地方（亮点）

1. **架构边界执行严格**：`builder/` 全目录无 `from core.harness` 直导（grep 0 命中），流水线执行收敛到 CoreFacade 或 PipelineOrchestratorClient HTTP 代理（`builder_project_service.py:26`），符合 platform→core 单向依赖契约。
2. **L2-L5 引擎分层纯净**：四个引擎文件全部纯标准库（ast/difflib/os/re），跨层访问唯一经 `CoreFacade.deploy_app_service`（`builder_project_service.py:1976`）；发布状态机走数据表 `_VALID_TRANSITIONS`（`release_engine.py:24-30`）、SQL 类型映射 `_TYPE_MAP`（`schema_migration.py:23-31`），无业务字符串判断。
3. **安全设计有亮点**：zip-slip 防护 + existing_path 白名单 + 敏感文件过滤（`builder_project_service.py:121-175`）、webhook hmac 常量时间比较（`builder_app_service.py:196`）、合并原子审批门禁 + sha256 并发快照校验（`builder_project_service.py:2111-2137`）。
4. **双模式纯配置驱动**：default.yaml 与 code.yaml 的全部差异（architecture_mode / test_execution_mode / uses_file_output / deploy_files_to_disk / input_artifacts）均由 YAML 字段表达，引擎只读字段（`pipeline_engine.py:4121-4124`），符合 §5.29 内核无关原则。
5. **测试体系分层完整**：L2 34 + L3 25 + L4 21 + L4.5 18 + L5 23 = 121 例 + 静态接线断言测试（test_l3_merge_static 等验证生产 caller）。

### 7.5.3 P0 级缺陷（5 个，已全部修复 2026-08-25）

> **修复状态**：5 个 P0 已全部修复并附回归测试（详见系统契约 §5.1、`aiPlat-core/core/tests/unit/test_pipeline_engine_p0_fixes.py`、`aiPlat-platform/tests/test_builder_p0_fixes.py`）。

| # | 缺陷 | 证据 | 影响 | 修复 |
|---|---|---|---|---|
| P0-1 | **start_pipeline 接线断裂**：BuilderProjectService 无 `start_pipeline_background` 定义，但 6 处生产调用点引用（workflow 执行、app 三模式、builder 路由 2 端点）→ 运行即 AttributeError | `builder_app_service.py:161,190,221`; `builder_workflow_service.py:214-215`; 全仓 grep 0 命中 def | 主执行路径可能瘫痪 | ✅ 新增 `start_pipeline`/`start_pipeline_background` 委托 `rebuild_project`（PRD 前置检查） |
| P0-2 | **eval(s) 任意代码执行**：`eval()` 直接执行 LLM 输出（PRD 解析兜底），prompt 注入可达任意代码执行 | `builder_project_service.py:958` | 安全高危 | ✅ 改 `ast.literal_eval`（+`import ast`） |
| P0-3 | **_deploy_result_files 路径穿越**：LLM 可控文件名直接 `os.path.join` 写出，可写任意路径；同文件 `_safe_join(396)` 未被使用 | `pipeline_engine.py:4685` | 安全高危 | ✅ `_safe_join(_target, _fname)` 约束，穿越跳过+告警 |
| P0-4 | **_prd 未定义**：域感知注入段引用未定义变量 `_prd`（AST 证实 0 赋值）→ profile=code 时必抛 NameError 且被 except 静默吞掉，DomainRouter/ContextBus 注入 100% 失效 | `pipeline_engine.py:4224,4244`; 4252-4253 静默吞错 | 功能失效 | ✅ 补 `_prd` 解析（`state.prd_data` → description 尾部 JSON → `{}`） |
| P0-5 | **部署签名 fail-open**：签名验证异常仅 warning 后继续部署（"部署签名验证失败，跳过"），安全检查形同虚设 | `builder.py:529-530` | 安全 | ✅ 改 fail-closed：异常/不通过均 `raise HTTPException(403)` |

### 7.5.4 P1 级问题（23 个，按主题归类）

- **God Class**：`BuilderProjectService` 类体 2287 行 / 80 方法，混杂 10+ 职责（`builder_project_service.py:425-2711`）；最长方法 `_deploy_to_app_for_project` 254 行（:2765-3016）
- **并行实现同一能力**：① PRD 解析两处（:1298 vs builder_session.py:285）——**✅ 已修复（2026-08-25）**，session 版委托 service 版唯一实现；② 节点→stage 转换两处（workflow_service:121 vs app_service:117）——**✅ 已修复（2026-08-25）**，抽取 `WorkflowService._nodes_to_stages` 唯一实现，app_service 委托；③ 状态读取三路径（HTTP/直读 SQLite/本地 JSON，:2254/2383/2473）④ HITL 审批三套
- **死代码/未接线**：builder_session.py 整文件（自述 deprecated）、builder_roles.py 零调用者、`_run_workflow`/`suspend_hitl` 等 0 调用者（违反 §9 接线规范）
- **引擎隐蔽 bug**：`_run_chained_skill` 引用未定义 `stage`（链式技能永不执行，:4597）——**✅ 已修复（2026-08-25）**，改用 `_chain_stage` 读超时；`_exec_test_runner` 写文件块在 continue 后不可达（被测代码永不落盘，pipeline_eval.py:260-266）——**✅ 已修复（2026-08-25）**，写文件块移至 continue 前；skip_pytest_gate 双份并行实现并伪造 APPROVED_SKIPPED 报告（:3958 vs pipeline_eval.py:198）——**✅ 已修复（2026-08-25）**，收敛到共享 `_apply_skip_pytest_gate`
- **前端/跨进程硬编码**：`http://localhost:8004` 4 处（builder.py:173 / AppPage.tsx:56 / Factory:494,2699）——**✅ 已修复（2026-08-25）**，后端 `_APP_BASE_URL` 环境变量 + 前端 `/app` 相对路径（vite proxy）；前端硬编码 artifact key（Factory:571,645 的 ['architecture','code','test_report']）
- **授权不一致**：create 需 admin（builder.py:105）但破坏性 delete 仅需 builder（:114,119）——**✅ 已修复（2026-08-25）**，delete/batch-delete 升级 require_admin_access
- **跨模块门禁误报**：verify_changed_module_contracts 仅扫 previews 新内容，漏再生文件被误判断裂（cross_module.py:213-247）——**✅ 已修复（2026-08-25）**，`_new_version_text` 纳入模块内未修改文件（传 module_root）
- **merge 部分失败误报 ok**：apply_merge failed 非空仍返回 status:'ok'（merge_engine.py:339-351）——**✅ 已修复（2026-08-25）**，failed 非空 → status=partial + detail
- **模板功能失效**：HITL 门禁合并对 dict 做属性访问（s.agent_id）→ AttributeError 被静默吞（builder_project_service.py:1234,1239-1240），v3.1 HITL gates 功能实际失效——**✅ 已修复（2026-08-25）**，改 dict 访问（.get）
- **§5.29 违反**：team_planner 用技能名关键词推断模型用途（:273-280）——⏳ 待修（需配置化声明 skill_model_purpose + 模型路由影响评估）

### 7.5.5 与"生成应用实证"的关联（原 §7.5 要点保留）

此前对 `~/.aiplat/apps/` 生成应用的分析（prj_c1b3c2d6 视频解析平台 213 文件 / FastAPI+React+Docker）仍有效。P0 修复后（2026-08-25）：P0-4 域感知注入已恢复（code 模式域分类生效）、P0-3 路径穿越已封堵——**原"部分 code 模式能力实际未生效"的判断已随修复解除**；生成应用 pass_rate=0 的根因（域注入失效）已消除，但 pass_rate 需在下次真实构建中复测确认。

### 7.5.6 修复优先级建议

1. **立即（P0）**：✅ 已完成（2026-08-25）——P0-2 eval 改 ast.literal_eval；P0-3 用 _safe_join 约束路径；P0-4 补 _prd 赋值；P0-5 签名失败改 fail-closed；P0-1 补 start_pipeline_background 定义
2. **本周（P1）**：拆分 BuilderProjectService God Class（按 CRUD/HITL/L2-L5/部署 4 职责）；统一状态读取三路径；修 HITL 模板属性访问；修 _exec_test_runner 不可达块
3. **跟踪（P2）**：19 个 P2（前端 catch 泛滥、死代码清理、测试陈旧更新等）

---

## 8. 总体评价

| 维度 | 星级 | 一句话 |
|---|---|---|
| 设计思路 | ⭐⭐⭐⭐⭐ | AI 判断需求类型自动选生产线，很聪明 |
| 能力真实度 | ⭐⭐⭐⭐ | 代码模式真生成文件、真跑测试，不是演示 |
| 工程完整度 | ⭐⭐⭐⭐⭐ | 从对话到部署全链路都有，还有审批/回滚/修复 |
| 透明度 | ⭐⭐⭐⭐ | 通过率已标来源，但估算精度仍有限 |
| 扩展性 | ⭐⭐⭐⭐ | 加新团队=加一个配置模板，不用改引擎 |

**总体判断**：应用工厂是 aiPlat 最接近"AI 代替人做软件"的能力——**真实可用、链路完整**。它的定位是"**快速搭轻应用**"（数据录入/查询/看板/问答），不是"替代专业开发做复杂系统"。对业务用户是极大提效，对技术用户是可配置的自动化产线。知道边界、按需使用，就是它的正确打开方式。

---

## 附录 A：核心代码证据（供技术核对）

| 结论 | 代码位置 |
|---|---|
| 双模式判断（mode: agent/code） | `aiPlat-core/core/harness/execution/team_planner.py:471-476` |
| 团队模板映射（agent→default.yaml / code→code.yaml） | `aiPlat-platform/builder/builder_project_service.py:1019` |
| 两套团队模板 | `~/.aiplat/teams/default.yaml`（agent）、`~/.aiplat/teams/code.yaml`（code） |
| 代码模式真实落盘 | `aiPlat-core/core/harness/execution/pipeline_engine.py:4398,4565`（`## FILE:` → `~/.aiplat/apps/{项目}/current/`） |
| 测试真实执行 | `aiPlat-core/core/harness/execution/pipeline_eval.py:165`（`_exec_test_runner`，含 ffmpeg 环境检查） |
| pass_rate 来源标注 | `aiPlat-platform/builder/builder_project_service.py:1787`（real_pytest/estimated + 原因） |
| 前端预置组件渲染（5 种） | `aiPlat-management/frontend/src/pages/App/AppPage.tsx:115-119` |
| 生命周期 API | `aiPlat-platform/api/routers/builder.py`（40+ 端点：chat/confirm/recommend-team/start/approve/reject/fix/rollback/deploy） |
| 部署目标 | aiPlat-app 端口 8004（`builder.py:336`） |
| L2 导入既有代码 | `builder_project_service.py` `import_repo`/`_safe_extract_zip`/`_scan_imported` + `core/harness/execution/pipeline_engine.py` `inject_imported_context` |
| L3 增量合并 | `builder/merge_engine.py`（ImpactAnalyzer/DiffMerger/原子审批/哈希锁/噪音折叠）+ `pipeline_engine.py` UNCHANGED 剔除 |
| L4 多模块编排 | `builder/cross_module.py`（CrossModuleAnalyzer/契约门禁）+ `builder_project_service.py` 模块 CRUD/编排 |
| L4.5 数据库迁移 | `builder/schema_migration.py`（SchemaExtractor/SchemaDiffAnalyzer/MigrationGenerator）+ 迁移端点 |
| L5 受控发布 | `builder/release_engine.py`（发布状态机/canary_weight）+ `core/harness/infrastructure/infra_bridge.py` deploy_app_service 桥接 |

## 附录 B：验证命令

```bash
# 双模式自动路由
grep -n "mode" aiPlat-core/core/harness/execution/team_planner.py
# 两套团队模板
ls ~/.aiplat/teams/   # default.yaml(agent) + code.yaml(code)
# 代码落盘 + 测试执行 + pass_rate 标注
grep -n "deploy_files_to_disk" aiPlat-core/core/harness/execution/pipeline_engine.py
grep -n "def _exec_test_runner" aiPlat-core/core/harness/execution/pipeline_eval.py
grep -n "pass_rate_source" aiPlat-platform/builder/builder_project_service.py
```
