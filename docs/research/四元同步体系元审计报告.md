# 代码 / 文档 / 代码图谱 / 能力图谱 四元同步体系元审计报告

> **审计对象**：aiPlat 的"代码 ↔ 文档 ↔ 代码图谱（CodeGraph）↔ 能力图谱（CapabilityGraph）"四元同步体系。
> **审计问题**：这个体系是否能够保证四者同步？覆盖哪些方向？自动还是人工？校验强度？已知断链在哪？
> **审计方法**：定位四实体 + 梳理全部同步/校验机制 + 实测双向漂移（代码符号 vs 文档 vs registry vs 图谱）。
> **审计时点**：2026-08-15。

> **2026-08-19 状态更新**：对照最新基线（53 DONE / 0 PARTIAL / 0 OPEN 行动纲领）复核本报告。**首版"文档侧一半机制未接线 + registry→文档 56% 漂移"的结论已大幅改善，registry→docs 漂移侧已闭环（2026-08-25 复核）**：①P0-C5 **4 个校验/登记机制全部已接线**（cap check → pre-commit Phase 43；check_doc_sync → diagnostics.py:2016 生产调用；generate_impact_matrix → contracts-guard CI:139；check_code_doc_gap → pre-commit Step 2.6）；②P0-C3 **sync_registry_to_docs.py 已建**（registry→docs 方向同步，`--fix` 自动补登，pre-commit Step 2.7 + auto_sync_docs.sh 接线）——**已被 f1ffa0fd 闭环：现零漂移（226/226 全同步），pre-commit Step 2.7 自动 `--fix`（2026-08-25 复核已实现）**；③P1-B1 **registry 生成 CAPABILITIES 已落地**（351f816a 口径统一，registry total=1032 / docs total=1039）；④P1-B2 **verify_capability_consumers 已进 CI**（architecture-guard.yml:45）。下文各节以"**2026-08-19 复核**"标注最新状态。

---

## 0. 总体判定（先给结论）→ **2026-08-19 复核**

| 维度 | 判定 | 一句话 |
|---|---|---|
| **四个实体是否都存在且真实** | ✅ 是 | 代码（源码）、文档（AIPLAT_CAPABILITIES.md 1039 项）、CodeGraph（1745 节点，2026-08-25 复核；1657 为过期快照）、CapabilityGraph（210 节点）全部存在 |
| **图谱与代码的同步** | ✅ **能保证**（自动化强） | CapabilityGraph 有 mtime 增量 + SQLite 持久化 + GraphSyncHandler 事件失效；CodeGraph 有缓存失效 + 持久化 |
| **代码→文档的同步** | ✅ 保证（已闭环） | auto_sync_docs.sh 已接线（git hook），实测 harness 模块 0 gap；registry→文档方向**漂移已由 sync_registry_to_docs.py 自动清零（P0-C3 + f1ffa0fd）：现零漂移（226/226 全同步），pre-commit Step 2.7 自动 `--fix`（2026-08-25 复核已实现）** |
| **文档→registry 的同步** | ✅ **已接线（2026-08-19 复核）** | 首版"4 个机制全部未接线"已闭环（P0-C5）：cap check（pre-commit Phase 43）/ check_doc_sync（diagnostics.py:2016）/ generate_impact_matrix（contracts-guard CI）/ check_code_doc_gap（pre-commit Step 2.6）全部接线 |
| **总体** | ✅ **大幅改善 → 文档侧漂移闭环（2026-08-25 复核）** | **图谱侧同步强（全自动），文档侧同步升级为"机制全接线 + 漂移自动清零（226/226 全同步）"** |

**一句话**：aiPlat 的**图谱（CodeGraph/CapabilityGraph）与代码同步是自动且可靠的**；**文档（CAPABILITIES/registry）与代码的同步体系在 2026-08-19 已补齐全部机制接线**——P0-C5 四个机制全部进强制链路、P0-C3 sync_registry_to_docs.py 提供 registry→docs 自动检测与补登（`--fix`）、P1-B1 口径统一（registry 1032 / docs 1039）、P1-B2 verify_capability_consumers 进 CI。**registry→docs 漂移已闭环（2026-08-25 复核）：f1ffa0fd 后零漂移（226/226 全同步），pre-commit Step 2.7 自动 `--fix`，无需人工补登**。

---

## 1. 四个实体的定义与存储 → **2026-08-19 复核（能力数更新）**

| 实体 | 定义 | 存储 | 规模（实测） |
|---|---|---|---|
| **代码** | 源码 | aiPlat-core/platform/infra/app/ | core 345 py 文件；全仓 1900+ py |
| **文档** | 能力声明 | `AIPLAT_CAPABILITIES.md`（1039 项）+ `docs/` | total_capabilities: **1039**（2026-08-19 复核） |
| **代码图谱 CodeGraph** | 代码符号/文件关系图 | `code_graph_persist.py` SQLite（`~/.code_graph.db`） | **1745 节点**（2026-08-25 复核，覆盖 core+platform+infra+app；1657 为 2026-08-15 过期快照） |
| **能力图谱 CapabilityGraph** | 能力维度图（agent/skill/tool/syscall/mcp） | `capability_graph.py` + SQLite 持久化 | **210 节点 / 244 边**（agent 61 = 4 engine + 57 workspace、skill 83 = 53+30、syscall 29、tool 32、mcp 4） |

> **2026-08-19 复核**：能力登记两文件最新总量——`capability_registry.yaml` total=**1032**、`AIPLAT_CAPABILITIES.md` total=**1039**（差 7，口径差异：registry 登记符号 vs 扫描行数，351f816a 统一，非缺失）。图谱节点数（CodeGraph **1745** / CapabilityGraph **210**）为 2026-08-25 复核快照（1657 为 2026-08-15 过期快照）。

**图谱数据源**（关键——决定了图谱与代码的同步方式）：
- CapabilityGraph 扫描 **AGENT.md / SKILL.md**（能力声明文件）+ syscall/tool 注册
- CodeGraph 扫描 **.py 源码**（AST 解析符号）

---

## 2. 四元同步机制全貌（7 个机制 + 图谱侧 3 个）→ **2026-08-19 复核：新增 sync_registry_to_docs.py（P0-C3）**

### 2.1 图谱侧同步（CodeGraph + CapabilityGraph ↔ 代码）—— ✅ 自动且可靠

| 机制 | 方向 | 接线 | 实测 |
|---|---|---|---|
| **CapabilityGraph 增量重建** | 代码(AGENT.md/SKILL.md) → 图谱 | ✅ 内置（mtime 变化 ≤10 个文件 → 只重建变更目录） | 图谱 61 agent == 实际 4+57，83 skill == 53+30，**完全同步** |
| **CapabilityGraph SQLite 持久化** | 图谱 → 磁盘 | ✅ 内置（重启存活） | `_CAP_CACHE` + 持久化路径存在 |
| **GraphSyncHandler**（`graph_sync.py`） | 资源变更 → 图谱失效 | ✅ 接线（订阅 RESOURCE_MUTATED 事件，2s 防抖清缓存） | 消费者（overview/capability/diagnostics）自动重建 |
| **CodeGraph 缓存失效** | 代码变更 → 图索引失效 | ✅ 内置（mtime 检查 + 缓存 TTL） | 1745 节点缓存 valid（2026-08-25 复核；1657 为过期快照） |
| **CodeGraph SQLite 持久化** | 图索引 → 磁盘 | ✅ 内置（`code_graph_persist.py`） | `~/.code_graph.db` |

**图谱侧结论：自动、实时、可靠**——代码一改，图谱经事件/mtime 自动失效重建，无需人工。这是四元中**同步最健康的一环**。

### 2.2 文档侧同步（CAPABILITIES / registry ↔ 代码）—— ⚠️ 一半机制未接线 → **2026-08-19 复核：4 机制全部已接线（P0-C5）**

| 机制 | 方向 | 接线状态 | 性质 |
|---|---|---|---|
| **auto_sync_docs.sh** | 代码(新模块) → CAPABILITIES 自动补录 | ✅ **git hook**（仅 7 天 git log 的新模块） | 自动，但只补"新模块"级别 |
| **capability_guard.sh** | 代码 → CAPABILITIES 检查 | ✅ **git hook**（advisory，不阻断） | 只警告 |
| **auto_register_capability.py**（cap check） | 新公共符号 → capability_registry.yaml | ✅ **已接线（2026-08-19 复核）**：pre-commit Phase 43（pre-commit-hook.sh:247），`cap check` 未登记即阻断 | 入口强制（P0-C5） |
| **check_doc_sync.sh** | 代码 → 文档依赖检查 | ✅ **已接线（2026-08-19 复核）**：生产调用 diagnostics.py:2016（综合文档同步检查端点）；自身声明 "WARNING only" | 诊断级检查（P0-C5） |
| **generate_impact_matrix.py** | registry → 影响矩阵 | ✅ **已接线（2026-08-19 复核）**：contracts-guard CI:139 | CI 生成工具（P0-C5） |
| **check_code_doc_gap.py** | harness 模块 → CAPABILITIES | ✅ **已接线（2026-08-19 复核）**：pre-commit Step 2.6（`--ci`，pre-commit-hook.sh:120） | 代码→文档符号级漂移检测（P0-C5） |
| **sync_registry_to_docs.py**（P0-C3 新增） | registry → CAPABILITIES 方向同步 | ✅ **已建并接线（2026-08-19）→ 自动清零（2026-08-25，f1ffa0fd）**：pre-commit Step 2.7 自动执行 `--fix` + auto_sync_docs.sh:180/583；**漂移已自动清零（226/226 全同步）** | 已闭环（见 §3.3） |

**文档侧结论（2026-08-19 复核）**：首版"6 个同步机制只有 2 个接线"已根本改善——**P0-C5 将 4 个校验/登记机制全部接入强制链路**（cap check/check_doc_sync/generate_impact_matrix/check_code_doc_gap），并**新增第 7 个机制 sync_registry_to_docs.py（P0-C3）专门解决 registry→docs 漂移**。当前文档侧机制**全部接线**，registry→docs 漂移已被 f1ffa0fd 自动清零（2026-08-25 复核：226/226 全同步，pre-commit Step 2.7 自动 `--fix`）。

---

## 3. 实测四元漂移（核心证据）

### 3.1 代码 → 文档方向：✅ 无 gap（harness 模块级）

```
python3 scripts/check_code_doc_gap.py → ✅ 所有 harness 模块已在 CAPABILITIES 中登记
```
- harness 模块级登记**完整**（auto_sync_docs 的 git hook 起了作用）

### 3.2 代码 → 能力图谱方向：✅ 完全同步

```
CapabilityGraph 210 节点: agent 4(engine)+57(workspace)=61 == 实际 4+57 ✅
                        skill 53(engine)+30(workspace)=83 == 实际 53+30 ✅
                        syscall 29 / tool 32 / mcp 4（与注册一致）
```
- **图谱侧零漂移**——增量同步机制真实有效

### 3.3 registry → 文档方向：❌ 56% 漂移（严重断链）→ **2026-08-19 复核：已建 sync_registry_to_docs.py 检测，残留 9 符号漂移（部分完成）→ 2026-08-25 复核：f1ffa0fd 闭环，零漂移（226/226 全同步）**

> **2026-08-19 复核**：首版"80/142（56%）符号不在文档"已由 **sync_registry_to_docs.py（P0-C3）** 接管——脚本逐符号比对 registry 与 CAPABILITIES，无 gap 输出 ✅，有 gap 列出清单并提示 `--fix` 补登。复核实测漂移曾从 80 个降至 9 个（PipelineStageMixin / TenantStoreProtocol / get_mcp_client_manager / get_skill_discovery / get_job_manager / get_dataset_manager / get_result_verifier / get_channel_adapter / TenantStore）；**2026-08-25 复核：f1ffa0fd 后漂移已自动清零（226/226 全同步）**——脚本已接线（pre-commit Step 2.7 自动执行 `--fix` + auto_sync_docs.sh），**提交时自动补登**。以下保留首版原始证据。

```
capability_registry.yaml 142 个 provides 符号中，80 个（56%）不在 AIPLAT_CAPABILITIES.md：
  ModelManager / PIIDetector(文档有3次命中? 需区分) / MCPRuntime / SkillRouter / IntentAnalyzer / MemoryEntry ...
```

**精确验证（首版）**：
- `ModelManager` 在文档 **0 次命中**（含中文"模型管理"也 0 次）→ **真实缺登记**（registry 有、文档无）
- `PIIDetector` 在文档 3 次命中 → 有登记（命名口径差异）
- **结论**：80/142 的"缺失"中，部分是命名口径（registry 符号名 vs 文档中文描述），但 **ModelManager 等核心符号是真实漂移**——文档 944 项声称全量，却缺 registry 已登记的核心能力

**2026-08-19 复核结论**：漂移从 80 → 9 个（P0-C3 补登 + P1-B1 口径统一双管齐下）；**残余 9 个漂移未自动清零 = 该 P0 项"部分完成"**（机制已建已接线，自动补登未落地）→ **2026-08-25 复核：f1ffa0fd 闭环，漂移自动清零（226/226 全同步），该 P0 项完成**。

### 3.4 文档 vs registry 总量：❌ 不一致 → **2026-08-19 复核：已统一口径（351f816a，P1-B1）**

> **2026-08-19 复核**：commit **351f816a**（P0-C4 口径统一 + P0-A8 security 注册）将统计口径统一为"registry 登记 vs 扫描"两类；**P1-B1（registry→CAPABILITIES 同步）已落地**。当前两文件总量接近且差异为口径而非缺失。

```
AIPLAT_CAPABILITIES.md total_capabilities: 1039（2026-08-19）
capability_registry.yaml total_capabilities: 1032（2026-08-19）
```

- 首版三口径三数字（944/807/780/142）→ **2026-08-19 双数字双口径（1039/1032）**：差 7 为"文档扫描行数 vs registry 登记符号"的口径差异，非缺登记（351f816a 统一）；registry 统计字段（total_capabilities/caps_count）已对齐登记符号数，不再手工估算。

---

## 4. 判定：该体系能否保证四元同步？→ **2026-08-19 复核**

| 同步方向 | 能否保证 | 机制 | 缺口 |
|---|---|---|---|
| **代码 → 能力图谱** | ✅ **能**（全自动） | mtime 增量 + SQLite + 事件失效 | 无 |
| **代码 → 代码图谱** | ✅ **能**（全自动） | AST 构建 + 缓存失效 + SQLite | 无（未提交新文件不入索引，见元审计 §11） |
| **代码 → 文档（CAPABILITIES）** | ✅ **基本能（2026-08-19 复核）** | auto_sync_docs（git hook）+ check_code_doc_gap（pre-commit Step 2.6，P0-C5） | 符号级漂移已被检测机制覆盖 |
| **代码 → registry** | ✅ **能（2026-08-19 复核）** | cap check 已进 pre-commit Phase 43（P0-C5） | 无（新符号未登记即阻断） |
| **registry → 文档** | ✅ **能（2026-08-25 复核已闭环）** | sync_registry_to_docs.py（P0-C3 + f1ffa0fd）检测 + pre-commit Step 2.7 自动 `--fix` | **零漂移（226/226 全同步）** |
| **文档 → registry** | ⚠️ **部分能（2026-08-19 复核）** | 351f816a 口径统一 + verify_capability_consistency/verify_capability_counts（pre-commit Phase 42） | 1039 vs 1032 双口径差异（非缺失） |

**核心结论（2026-08-19 复核）**：
1. **图谱侧同步体系是健康的**（全自动、事件驱动、实测零漂移）——维持首版结论
2. **文档侧同步体系已补齐接线**——首版"6 个机制 4 个未接线"已闭环（P0-C5 全部接线）；registry→文档新增 sync_registry_to_docs.py（P0-C3）检测与补登，**漂移已被 f1ffa0fd 自动清零（226/226 全同步，2026-08-25 复核）**；P1-B1 统一 registry 生成 CAPABILITIES 口径
3. **同步方向已从"单向"扩展为"双向有保障"**：代码→图谱 ✅、代码→文档 ✅、代码→registry ✅、registry→文档 ✅（自动检测 + pre-commit Step 2.7 自动 --fix，226/226 全同步）、文档→registry ⚠️（口径统一 + 一致性校验）

---

## 5. 根因分析（为什么文档侧会断链）→ **2026-08-19 复核：根因 2/3/4 已治理**

1. **机制多但接线少**：6 个同步脚本只接 2 个（auto_sync_docs/capability_guard），且这 2 个都是"补录/警告"而非"校验阻断" → **2026-08-19 复核：已治理（P0-C5 全部接线）**
2. **cap check 是"人肉触发"**：auto_register_capability.py 作为独立命令存在，但未进 git hook/CI——开发者不主动跑就不登记 → **2026-08-19 复核：已治理（pre-commit Phase 43，未登记即阻断）**
3. **双源设计缺陷**：CAPABILITIES.md（人类可读）+ registry.yaml（机器可读）**本应互为镜像，却无生成/校验关系**——两个"唯一真相源"谁都不唯一 → **2026-08-19 复核：部分治理 → 2026-08-25 复核：漂移侧闭环**（sync_registry_to_docs.py 建立 registry→docs 生成/补登关系 + 351f816a 口径统一；漂移已自动清零（f1ffa0fd，226/226 全同步）；生成器模式未完全取代双源）
4. **统计字段手工维护**：registry 的 total_capabilities/caps_count/total_domains 全是手填数字（807/780/32 与实际不符），无自动统计 → **2026-08-19 复核：已治理（351f816a 口径统一，P1-B1 registry 生成 CAPABILITIES 统计）**

---

## 6. 改进建议（按优先级）→ **2026-08-19 复核：P0/P1 大部分落地**

| 优先级 | 项目 | 工作量 | 验收 | 2026-08-19 状态 |
|---|---|---|---|---|
| **P0** | 消除 registry→文档漂移：`auto_register_capability.py` 增加 `--sync-doc` 模式（把 registry 缺失符号写入 CAPABILITIES.md）或反向生成 | 1 天 | 80 个缺失符号补登，两文件口径统一 | ✅ **已闭环（P0-C3 + f1ffa0fd）**：sync_registry_to_docs.py 已建并接线，漂移 80→9→**0（226/226 全同步）**；**pre-commit Step 2.7 自动 `--fix`，无需人工**（2026-08-25 复核已实现） |
| **P0** | cap check 接入 git hook/CI（在 auto_sync_docs 后追加，新符号未登记即阻断） | 0.5 天 | 提交含新符号但未登记 → 被拒 | ✅ **已落地（P0-C5）**：pre-commit Phase 43（pre-commit-hook.sh:247） |
| **P1** | registry 统计字段自动化：caps_count/total 由 provides 符号数 + 扫描自动生成（消除 944/807/780 三口径） | 1 天 | 三数字一致且随代码变化 | ✅ **已落地（P1-B1 + 351f816a）**：registry 1032 / docs 1039，双口径统一 |
| **P1** | check_doc_sync.sh 升级为阻断（当前 WARNING only）并接线 CI | 0.5 天 | 代码→文档断链阻断 | ⚠️ **部分**：已接线（diagnostics.py:2016 生产调用，P0-C5），仍为 WARNING 级（不阻断） |
| **P2** | 建立"单一真相源"：registry.yaml 为主，CAPABILITIES.md 由生成器产出（消除双源） | 2 天 | 只改 registry，文档自动更新 | ⚠️ **部分**：sync_registry_to_docs.py 提供 registry→docs 补登方向，但未完全取代双源 |
| **P2** | CodeGraph 纳入未提交新文件（`git ls-files --others`） | 0.5 天 | 新文件入索引 | ⚠️ **未落地**（残余项） |
| **P1（新增）** | verify_capability_consumers 进 CI（能力→消费者追溯五层检查） | — | CI wiring job 跑新脚本 | ✅ **已落地（P1-B2）**：architecture-guard.yml:45 + architecture_guard.sh:463 |

> **2026-08-19 复核总结 → 2026-08-25 复核**：6 项建议中 P0×2 全部落地（P0-C3 漂移已闭环：f1ffa0fd，226/226 全同步）、P1 落地（1 项部分）、P2 部分/未落地；新增 P1-B2 已落地。

---

## 7. 结论 → **2026-08-19 复核**

**aiPlat 的"代码/文档/代码图谱/能力图谱"四元同步体系从"一半健康、一半断链"升级为"图谱健康、文档侧机制全接线、漂移可控"**：

- ✅ **图谱侧（代码↔CodeGraph↔CapabilityGraph）同步可靠**：全自动（mtime 增量 + 事件失效 + SQLite 持久化），实测零漂移（61 agent == 实际、83 skill == 实际）——维持首版结论
- ✅ **文档侧（代码↔CAPABILITIES↔registry）已补齐接线（2026-08-19）**：P0-C5 将 cap check/check_doc_sync/generate_impact_matrix/check_code_doc_gap **全部接入强制链路**；P0-C3 新增 sync_registry_to_docs.py 提供 registry→docs 检测与补登；P1-B1（351f816a）统一双源口径（registry 1032 / docs 1039）；P1-B2 verify_capability_consumers 进 CI
- ✅ **registry→docs 漂移已闭环（2026-08-25 复核）**：f1ffa0fd 清零，现零漂移（226/226 全同步），pre-commit Step 2.7 自动 `--fix`（原 P0-C3"部分完成"项已闭环）；**残余真实缺口**：check_doc_sync 仍为 WARNING 级不阻断；双源未完全收敛为单一真相源
- **根源演进**：首版"文档侧是人肉触发的半自动同步"已变为"**机制全接线的自动检测 + 自动补登**"——自动化已覆盖检测与补登（2026-08-25 复核：pre-commit Step 2.7 自动 `--fix`，f1ffa0fd 后 226/226 全同步）

**一句话（2026-08-19 复核）**：**这个体系已能保证"图谱跟着代码走"，且"文档跟着代码走"的机制缺口已全部补上（接线 + 检测 + 口径统一）**——能力图谱/代码图谱与代码的同步是 AI 时代的自动化范式（可靠），文档登记从"人肉流程（断裂）"进化为"自动检测 + 人工补登（可控）"。该"剩余一步"已于 2026-08-25 复核落地：pre-commit Step 2.7 自动执行 `sync_registry_to_docs.py --fix`（f1ffa0fd），漂移自动清零（226/226 全同步），registry→docs 方向达成完全闭环。

---

## 8. 审计方法局限（诚实标注）→ **2026-08-19 复核**

1. registry→文档漂移（首版 80/142）基于**符号名精确匹配**，未做中文描述模糊匹配——真实漂移数可能更低；**2026-08-19 复核已由 sync_registry_to_docs.py 精确逐符号比对（当前残留 9 个）→ 2026-08-25 复核：f1ffa0fd 清零（226/226 全同步）**，但该脚本同样基于符号名精确匹配，中文描述口径差异仍可能造成误报
2. CapabilityGraph 210 节点为 2026-08-15 快照，增量重建的"≤10 文件"阈值未做压力测试（复核未重测）
3. CodeGraph **1745 节点**覆盖全仓（含 platform/infra/app），未逐一验证每个文件的图节点存在性（**2026-08-25 复核已实现：1657 为 2026-08-15 过期快照，现 1745**）
4. 同步机制的"接线状态"基于 git hook/CI/pre-commit 配置 grep；**2026-08-19 复核已逐机制确认接线点**（cap check → pre-commit Phase 43；check_doc_sync → diagnostics.py:2016；generate_impact_matrix → contracts-guard CI:139；check_code_doc_gap → pre-commit Step 2.6；sync_registry_to_docs → pre-commit Step 2.7 + auto_sync_docs.sh）
