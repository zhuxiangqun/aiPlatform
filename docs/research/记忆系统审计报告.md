# aiPlat 记忆系统审计报告（2026-08-23）

> **最后验证： 2026-09-06（复核）**（verify 命令：grep 四层实现/接线调用点/semantic 字段；测试在 CI core job 运行；2026-08-23 复核：freshness 检查器新增 aiPlat-app 搜索根，不影响本报告结论；2026-08-25 复核：事件源纯度 fork 会话实施（pipeline_run_store/pipeline_execution）不影响本报告记忆结论）
> **范围**：`aiPlat-core/core/harness/memory/`（8,414 行，23 个文件）——六层决策操作系统 L2 动态感知的核心载体。

---

## 1. 审计结论（TL;DR）

**记忆系统架构完整、关键机制齐备，接线到位**。四层架构（Working/Episodic/Semantic/Task Skills）与 CLAUDE.md §5.28 一致；5 级压缩、语义过期、投毒防御（source_tag/trust_weight/provenance）等治理机制均在代码中实证。发现 2 项非阻塞问题（均非本次引入）。

| 维度 | 结果 |
|---|---|
| 四层架构 | ✅ working.py / episodic.py / semantic.py / manager.py（TaskSkill） |
| 5 级压缩 | ✅ compression.py NORMAL→EMERGENCY（0-1.0 阈值） |
| 语义过期/软删除 | ✅ semantic.py expires_at / is_deleted（Phase 40） |
| 投毒防御 | ✅ semantic.py source_tag / trust_weight / provenance |
| Reminders | ✅ reminders.py + graph_injector.py:216 接线 |
| 接线 | ✅ loop/_facade.py:1358 `_try_save_interaction` + compressor.py:227 |
| 测试 | ⚠️ 本地旧路径（`harness.memory.*`）需特定 PYTHONPATH；CI core job 正常 |

---

## 2. 四层架构验证

| 层 | 实现 | 关键方法 | 状态 |
|---|---|---|---|
| **Working（热）** | `working.py`（109 行） | deque 滑动窗口 | ✅ |
| **Episodic（温）** | `episodic.py` | 规则摘要（预评分） | ✅ `manager.py:1399 export_episodic_state` |
| **Semantic（冷）** | `semantic.py`（SQLite + FTS5） | expires_at/is_deleted/source_tag/trust_weight/provenance | ✅ |
| **Task Skills（外挂）** | `manager.py:1610 save_task_skill` | TaskSkill → `~/.aiplat/task_skills/` JSON + SkillRegistry | ✅ |

**与 Hermes 四层对应**：Hot→Working / Warm→Episodic / Cold→Semantic / External→Task Skills（对标报告 §5.28 已映射）——架构对齐良好。

---

## 3. 治理机制验证

| 机制 | 代码实证 | 状态 |
|---|---|---|
| 5 级压缩（70%→99%） | `compression.py:141-151` NORMAL(0-0.85)/WARNING(0.85-0.90)/REPLACE(0.90-0.93)/PRUNE(0.93-0.96)/AGGRESSIVE(0.96-0.99)/EMERGENCY(0.99-1.0) | ✅ |
| 语义过期自动续期 | `semantic.py:25` expires_at + 清理条件 | ✅ |
| 软删除可恢复 | `semantic.py:26` is_deleted | ✅ |
| 投毒防御三字段 | `semantic.py:27-29` source_tag/trust_weight/provenance | ✅ |
| Transcript Guard | `transcript_guard.py`（60 行）role 归一化 | ✅ |
| Reminders 注入 | `graph_injector.py:216` get_reminders + reminders.py | ✅ |
| 交互持久化 | `loop/_facade.py:1358-1399` `_try_save_interaction` → `mgr.save_interaction` + `compressor.py:227` | ✅ |

---

## 4. 接线完成度

| 调用链 | 证据 | 状态 |
|---|---|---|
| Agent 循环 → save_interaction | `loop/_facade.py:1399` | ✅ |
| 压缩路径 → save_interaction | `loop/compressor.py:227` | ✅ |
| 循环 → reminders | `loop/graph_injector.py:216` | ✅ |
| Pipeline → episodic 导出 | `builder_project_service.py _save_state`（`export_episodic_state`） | ✅ |
| Task Skill 晶体化 | `pipeline_engine.py _crystallize_skill` → save_task_skill | ✅ |

---

## 5. 已知非阻塞问题

| # | 问题 | 类型 | 建议 |
|---|---|---|---|
| 1 | 记忆单元测试用旧导入路径 `harness.memory.*`（非 `core.harness.memory`）——本地直接 pytest 报 ModuleNotFoundError，需特定 PYTHONPATH 或 CI 环境 | ⚠️ 存量测试债务 | 迁移测试导入到 `core.harness.memory.*`（与代码一致），或 CI 保持现有路径配置 |
| 2 | `loop/` 目录下直接 syscall 调用 3 处（reason/act/observe nodes）为已知例外（CLAUDE.md §5.23 标注 Phase 9 后 retire）——与记忆系统间接相关 | ⚠️ 存量架构债务 | 保持现状（已登记） |

---

## 6. 结论与建议

1. **记忆系统健康**：四层架构 + 治理机制 + 接线全链路实证，与 Hermes 四层框架对齐（对标报告已映射）。
2. **建议 A（低优先）**：迁移记忆测试导入路径到 `core.harness.memory.*`，消除本地运行障碍。
3. **建议 B（观察）**：semantic 记忆的续期/清理策略（expires_at + access_count）可在真实负载下验证参数是否合理——需用户数据。
4. **建议 C（已覆盖）**：Task Skill 晶体化（pass_rate≥85% 自动注册 SkillRegistry）与应用工厂 L5 的受控发布是"经验复用"的两条路径，可考虑联动（发布成功的高通过率 Skill 自动晶体化）。
