# aiPlat 系统健康审计报告（L2-L5 演进后，2026-08-23）

> **最后验证：2026-08-23**（verify 命令：bash scripts/architecture_guard.sh / `pytest aiPlat-core/core/tests/constitution/ tests/constitution/` / python3 scripts/check_research_docs_freshness.py .）
> **范围**：应用工厂 L2→L5 大工程（PR #77-#99，23 个 PR）合并后的全系统健康状态复核。

---

## 1. 审计结论（TL;DR）

**系统健康，可继续演进。** L2-L5 演进（导入既有代码 → 增量合并 → 多模块编排 → 数据库迁移 → 受控发布）全部落地并受守卫链保护，无新引入的架构违规；存在 1 项存量非阻塞测试债务（`test_v27_modules.py`，环境相关）。

| 维度 | 结果 |
|---|---|
| 架构守卫（architecture_guard.sh） | ✅ exit 0（129 PASS，0 FAIL） |
| 引擎守卫（pre-commit-engine-guard） | ✅ clean（R1-R4） |
| 宪法测试（constitution） | ✅ core 12 + root 21 = 33 passed |
| L2-L5 能力测试 | ✅ **124 passed**（platform）+ 12（core L2 + freshness） |
| 文档新鲜度（Rule 6） | ✅ 全绿（research 引用符号全部存在） |
| 前端 | ✅ tsc + build 通过 |
| 契约体系 | ✅ acceptance 至 1.52 + 鉴权规范 §19 + run spec 五十一轮 |

---

## 2. 守卫链验证

| 检查 | 命令 | 结果 |
|---|---|---|
| 架构守卫（§1-§93） | bash scripts/architecture_guard.sh | ✅ 129 PASS / 0 FAIL |
| 引擎守卫（R1-R4） | bash scripts/pre-commit-engine-guard.sh | ✅ clean（无中文 prompt/无硬编码 artifact key/无绕过） |
| 宪法 AST（无硬编码业务 key） | pytest tests/constitution/test_ast_business_keys.py | ✅ 2 passed |
| 层边界（platform→core→infra 单向） | pytest tests/constitution/test_layer_boundaries.py | ✅ 21 passed |
| 引擎通用性/去业务化 | pytest aiPlat-core/core/tests/constitution/ | ✅ 12 passed |
| 文档新鲜度（Rule 6） | python3 scripts/check_research_docs_freshness.py . | ✅ OK |

**架构违规修复记录**（演进期间守卫拦截后修复，均已闭环）：
- L5 v1 `platform 直导 infra`（违反 platform→core→infra）→ L5 v2 经 `CoreFacade.deploy_app_service` → `infra_bridge` 桥接 ✅
- L2 `silent except:pass` ×2 → noqa 标注 ✅
- L2/L3 `state` key 越界 → engine_state_keys baseline + constitution FRAMEWORK_KEYS 白名单 ✅

---

## 3. L2-L5 演进能力验证（124 例）

| 层 | 测试数 | 关键覆盖 |
|---|---|---|
| L2 导入既有代码 | 30（platform）+ 4（core） | zip-slip 拒绝/密钥过滤/manifest/has_tests/missing_deps/意图校验/prompt 注入/skip_gate |
| L3 增量合并 | 25 | ImpactAnalyzer/DiffMerger/原子审批/哈希锁/接口 AST 检测/噪音折叠/UNCHANGED |
| L4 多模块编排 | 21 | 三类契约依赖/影响闭包/拓扑顺序/契约门禁/模块接线 |
| L4.5 数据库迁移 | 18 | SchemaExtractor（SQLAlchemy/Pydantic）/destructive 判定/up-down 成对 |
| L5 受控发布 | 23 | 版本化产物/状态机流转/canary_weight/facade 桥接 |
| **合计** | **124** | （另 L2 core 4 + freshness 8 = 136 全绿） |

**演进闭环**：`AI 改模型 → 增量合并审批（L3）→ 数据库迁移（L4.5）→ 版本化发布（L5）→ 金丝雀验证 → 全量/回滚`——代码、schema、发布三者同步受控演进（详见 `应用工厂分析报告.md` §9）。

---

## 4. 契约体系状态

| 契约文档 | 最新轮次/条目 | 说明 |
|---|---|---|
| acceptance-contract | **1.52** | L2-L5 每轮自动化验收项（含计划文档存在性/符号计数/测试数） |
| 鉴权规范 | **§19** | L2-L5 端点权限登记（import/merge/迁移/发布/analyze-impact） |
| run spec | **五十一轮** | L2-L5 每次 core/harness 变更的落地记录 |
| boundary（01-architecture-contract） | L5 v2 追加 | 引擎零业务文案原则 + infra 桥接唯一入口 |

---

## 5. 已知非阻塞项（存量，非本次引入）

| 项 | 状态 | 说明 |
|---|---|---|
| `test_v27_modules.py` 存量失败 | ⚠️ 非阻塞 | readonly-db 环境相关（需 DB 写入）；不影响 CI 主链 |
| e2e-docker `database is locked` | ⚠️ 偶发 | 环境竞争（state_history 启动建库）；gh run rerun --failed 即可恢复（本次多轮验证中均 rerun 通过） |
| verify_imports 4 个缺失模块 | ⚠️ 存量 | `channels.adapter`/`custom_handlers` 等，与 L2-L5 无关 |

---

## 6. 后续建议

1. **L2 埋点复盘**：`GET /import-stats` 的 skip_pytest_gate 比率 >40% 触发 L3 优先级告警——需真实用户数据才能评估（当前为功能就绪态）。
2. **L5 v3**：真实流量路由金丝雀（依赖部署环境/网关支持）+ 跨环境发布——等实际部署场景再定。
3. **契约体系收敛**：acceptance 已达 1.52、run spec 五十一轮——若增长过快可考虑按子系统归档（v2 治理项）。
