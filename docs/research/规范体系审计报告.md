# aiPlat 规范体系审计报告（规范是否正确、完整、够用）

> **审计对象**：aiPlat 文档系统中的规范体系——docs/standards（9 份，全部 approved）+ docs/policy + docs/compliance + docs/security + architecture/boundary_rules.yaml + 5 份 CLAUDE.md 的强制规则（核心 105 条 §5.x）+ arch_guard_rules.yaml（190 条）。
> **审计问题**：这些规范是否正确、完整？针对 aiPlat 系统是否够用？
> **审计方法**：全量定位 → 逐份阅读 → 规范 vs 代码事实对照 → 维度覆盖盘点 → 系统风险点盲区检测。
> **审计时点**：2026-08-15；**2026-08-19 状态更新**：对照基线 `/tmp/status-baseline-2026-08-19.md` 复核，本报告所有"draft/盲区/缺失/执行断裂"结论已按最新状态更新（详见 §0/§2/§3/§4/§5）。关键变化：
> - **P0-C1**：standards 3 份 draft → **9/9 全部 approved**（3 份原规范 approved_date 2026-08-16；6 份新规范 approved_date 2026-08-18）
> - **P0-C2**：两项规范矛盾**已消歧/已实现**——① mTLS vs API key：鉴权规范 §1.2 消歧，**以代码为准**（JWT + API key，mTLS 降级为远期目标）；② request_dedup：**已实现**（`execution_store/runs_mixin.py` `get_run_id_for_request`/`remember_request_run_id` + schema migration v22，tenant 感知幂等映射）
> - **P1-B4**：前端规范 approved（**325** 文件有约束 + guard §43-47 + tsc 0 错误）
> - **P1-B5**：数据/DB 规范 approved
> - **规范-独立多租户 / 规范-性能 / 规范-错误处理 / 规范-命名 已定稿**（均 approved）
> - **执行度**：24 项宪法违规**已清零**（宪法测试 143 passed + 1 skipped 全绿；`architecture_guard.sh` 0 ERROR）

---

## 0. 总体判定（先给结论）

> **2026-08-19 更新**：本表为复核后状态。原 2026-08-15 判定中"全部 status: draft / 六大维度规范缺失 / 盲区 / 执行断裂"均已被最新状态取代。

| 维度 | 判定 | 一句话 |
|---|---|---|
| **正确性** | ✅ 已定稿 | 规范内容本身合理（三 ID/鉴权/session 设计专业），且 **9/9 全部 status: approved**（原 3 份 draft 已定稿，approved_date 2026-08-16）；**规范 vs 代码矛盾已消歧**（mTLS vs API key 以代码为准、request_id/run_id 命名空间消歧、request_dedup 已实现） |
| **完整性** | ✅ 核心丰富、外围已补齐 | aiPlat-core/CLAUDE.md **105 条规范**覆盖引擎/记忆/知识/Agent/Skill/工具等核心；standards/ 由 3 份扩至 **9 份**，**数据/DB、性能、命名、错误处理、前端、独立多租户规范全部补齐并 approved**（P1-B4/B5 + P2-B3） |
| **够用性（针对 aiPlat）** | ✅ **已覆盖** | 规范覆盖"内核/架构"充分，系统实际风险点已逐一闭环：①守卫 12 条 bug 已修复（93b7c25c）+ 黄金样本接入 CI（P0-C7）；②能力登记双源漂移有 sync_registry_to_docs.py（P0-C3，漂移未自动清零为部分残留）；③前端 325 文件已有规范约束（guard §43-47 + tsc 0 错误）；④多租户/计费已有专门规范 |
| **执行度** | ✅ 链路已闭环 | 规范→规则→守卫链路已打通：**24 项宪法违规清零**，宪法测试 **143 passed + 1 skipped** 全绿，`architecture_guard.sh` **0 ERROR**（§17 PytestCheck warn 级放行） |

**一句话**：aiPlat 的规范**内核强**（105 条 engine 规范 + 190 守卫规则，是我见过的 agent 系统中最系统的），且 **2026-08-16/18 已补齐外围并全量定稿**：① 9/9 standards 规范 approved（含前端/数据/性能/错误处理/多租户/命名）；② 规范 vs 实现矛盾全部消歧（mTLS 降级远期、request_dedup 已实现）；③ 规范→执行链路闭环（24 项违规清零、宪法测试全绿、守卫 0 ERROR）。**剩余少量残留**：日志格式/级别/脱敏仍无专门规范；registry→CAPABILITIES 漂移未自动清零（P0-C3 部分）。

---

## 1. 规范清单全貌（按来源）

| 来源 | 数量 | 内容 | 状态 |
|---|---|---|---|
| **docs/standards/** | **9 份** | ① 规范-core-run_id-trace_id-request_id（三 ID 分离+幂等）② 规范-platform-鉴权与身份透传（JWT+API key，mTLS 远期）③ 规范-app-session_id与conversation_key（会话边界）④ 规范-前端架构与工程（P1-B4）⑤ 规范-数据与数据库（P1-B5）⑥ 规范-独立多租户 ⑦ 规范-性能 ⑧ 规范-错误处理 ⑨ 规范-命名（⑥-⑨ 为 P2-B3 补充） | **9/9 全部 status: approved**（原 3 份 approved_date 2026-08-16；新 6 份 approved_date 2026-08-18） |
| **docs/policy/** | 2 份 | external-allowlist、mcp-stdio-prod-allowlist | 明确 |
| **docs/compliance/** | 1 份 | eu-ai-act-assessment（欧盟 AI 法评估） | 明确 |
| **docs/security/** | 4 份 | security-architecture、security-testing、ai-pentest、pen-test-report | 明确 |
| **architecture/boundary_rules.yaml** | 1 份 | 层边界规则（数据化） | 明确 |
| **根 CLAUDE.md** | §0-22 | 架构守卫铁律/内核无关/接线/成本阶梯/贡献红线/Prompt Cache 等 | 强制 |
| **aiPlat-core/CLAUDE.md** | **105 条 §5.x** | 依赖/配置驱动/单向依赖/门面/Agent/Skill/工具/记忆/注入防护/副作用/可观测性/上下文预算/压缩阈值/提示词模板等 | 强制 |
| **arch_guard_rules.yaml** | 190 条 | grep 规则（grep_forbidden 107/grep_required 44/cmd_output 29 等，含前端 §43-47） | 强制 |

**规范总量**：9（standards，全部 approved）+ 2（policy）+ 1（compliance）+ 4（security）+ 105（core CLAUDE）+ 190（守卫规则）+ 根 CLAUDE §0-22 = **约 300+ 条规范/规则**——**量是充足的，且定稿率 100%（standards）**。

---

## 2. 正确性评估

### 2.1 ✅ 规范内容本身合理（抽样）

| 规范 | 内容质量 | 证据 |
|---|---|---|
| 三 ID 规范 | 分工清晰（request_id 幂等/run_id 生命周期/trace_id 观测）、幂等映射表设计、贯穿字段明确 | 规范正文 §1-3 |
| 鉴权规范 | JWT claims 最小集表、request_id 不进 JWT 的论证（"JWT 是可复用凭证"）、透传 header 规范 | 规范正文 §2-3 |
| session_id 规范 | "会话边界应绑定业务上下文容器而非用户"（防串话/越权）——**设计判断正确** | 规范正文 §1-2 |

### 2.2 ✅ 规范 vs 代码事实的矛盾（P0-C2 已全部消歧/已实现）

> **2026-08-19 更新**：原 C1-C3 三项矛盾在 P0-C2（2026-08-16 定稿消歧）后全部闭环，消歧记录已写入规范正文（`规范-platform-鉴权与身份透传.md` §1.2、`规范-core-run_id-trace_id-request_id.md` §1.1）。

| # | 规范要求 | 代码实际 | 现状（2026-08-19） |
|---|---|---|---|
| C1 | 鉴权规范："服务间鉴权优先 mTLS；若必须用 token 用 service JWT" | `authenticator.py` 实际用 **API key**（`apl_` 前缀 + sha256） | **✅ 已消歧**（P0-C2）：规范 §1.2 明确"**以代码为准**：当前实现为 JWT/API key；mTLS 降级为远期目标，实现后方可回迁'优先'" |
| C2 | 三 ID 规范："request_id ≠ run_id（幂等分离）" | `job_scheduler.py:273` `request_id=run_id` | **✅ 已消歧**（P0-C2）：规范 §1.1 补充"job 调度 run_id 与 pipeline run_id 命名空间分离（`jobrun-` 前缀），不可混用"；pipeline 侧 run_id 为 `run-<uuid12>`（`core_facade.py:1868`），格式以代码为准 |
| C3 | 三 ID 规范："request_dedup 幂等映射表" | 原未发现实现 | **✅ 已实现**（P0-C2）：`core/services/execution_store/runs_mixin.py` `get_run_id_for_request`（`(tenant_id, request_id)` 幂等查询）+ `remember_request_run_id`（`ON CONFLICT DO NOTHING` 写入）+ schema migration v22 建表 `request_dedup(tenant_id, request_id, run_id, created_at)` |

### 2.3 ✅ 规范执行状态（24 项宪法违规已清零）

> **2026-08-19 更新**：原 2.3"规范未被完全执行（24 项违规）"已全部修复——宪法测试 **143 passed + 1 skipped** 全绿（含 test_core_internal_boundaries 3 / kernel_agnostic / layer_boundaries / infra_agnostic 11 / platform_function_boundary / ast_business_keys / system_prompt_flow 5 / sysgraph_registration 5 等），`architecture_guard.sh` **0 ERROR**。P0-A 架构合规 10/10 DONE。

| 规范条款（原违规） | 修复情况（2026-08-19） |
|---|---|
| core CLAUDE §5.7 单向依赖/门面（原 harness→apps import 53 处） | **✅ 已清零**（P0-A1）：9 处服务调用经 integration.py DI（宪法白名单 38→25） |
| core CLAUDE §5.7 门面使用（原 api→engine 直导 61 处） | **✅ 已清零**（P0-A2）：54 文件 292 行 harness 直导改为经 `core.api.core_facade`（CoreFacade 补 sys_llm_generate re-export） |
| 根 CLAUDE §8 内核无关（原 kernel phase 字符串分支 3 处） | **✅ 已清零**（P0-A5/A6）：infra 硬编码已修（8c6a5154）；ast_business_keys 绿（R4 正则 + engine_state_keys 基线） |
| core CLAUDE §5.2 Schemas/提示词模板（原模板占位符 `{}`） | **✅ 已清零**（P0-A7）：system_prompt_flow 绿 |

**结论**：规范**写得对**（条款正确），且 **2026-08-16/18 执行链路已闭环**——原 24 项宪法违规全部清零，宪法测试入 CI（P0-C6），守卫 12 条语法 bug 已修复（93b7c25c）并接入黄金样本验证（P0-C7：`rule_golden_sample.py --verify` 进 architecture-guard.yml CI）。

---

## 3. 完整性评估（按维度覆盖）

### 3.1 ✅ 已覆盖的维度（规范充足）

| 维度 | 规范来源 | 强度 |
|---|---|---|
| 架构/边界/依赖 | core CLAUDE §5.1/5.7 + boundary-standard + boundary_rules.yaml + 190 守卫规则 | ★★★★★ |
| 内核无关（去业务化） | 根 CLAUDE §8 + arch_guard §80-88 | ★★★★★ |
| 引擎/执行 | core CLAUDE §5.4-5.8（配置驱动/双引擎/退化策略） | ★★★★★ |
| Agent/Skill/Tool | core CLAUDE §5.9-5.11 + §17 Skill 真实性 | ★★★★★ |
| 记忆/上下文 | core CLAUDE §5.12/5.21/5.25/5.28 | ★★★★★ |
| 安全（注入防护） | core CLAUDE §5.18 + security/ 4 份 | ★★★★☆ |
| 模型管理 | 根 CLAUDE §12/14（唯一真相源） | ★★★★☆ |
| 身份/会话/三 ID | standards/ 3 份（已定稿 approved） | ★★★★★（approved） |
| **前端（TS/React）** | **规范-前端架构与工程（P1-B4，approved）**：guard §43-47（vite proxy / 跨语言 API 契约 / 路径契约 / import hygiene）+ tsc 0 错误 | ★★★★☆ |
| **数据/DB** | **规范-数据与数据库（P1-B5，approved）**：schema 归属/迁移/命名 | ★★★★☆ |
| **性能** | **规范-性能（P2-B3，approved）**：QPS/延迟/资源预算 | ★★★★☆ |
| **错误处理** | **规范-错误处理（P2-B3，approved）** | ★★★★☆ |
| **独立多租户** | **规范-独立多租户（P2-B3，approved）**：隔离/配额/套餐 | ★★★★☆ |
| **命名** | **规范-命名（P2-B3，approved）** | ★★★★☆ |

### 3.2 ✅ 原缺失维度（2026-08-16/18 已全部补齐）

> **2026-08-19 更新**：原 3.2"六大维度规范盲区"已全部覆盖——standards/ 由 3 份扩至 9 份，新增 6 份全部 approved（P1-B4 前端、P1-B5 数据、P2-B3 独立多租户/性能/错误处理/命名）。**唯一残留**：日志规范仍无专门 standards 文档（trace_id 贯穿有规范，但日志格式/级别/脱敏无统一标准）。

| 原缺失维度 | 现状（2026-08-19） | 判定 |
|---|---|---|
| **数据/DB 规范** | **规范-数据与数据库.md**（P1-B5，approved 2026-08-18）；tenant_quotas 等表已迁 platform `TenantStore`（P0-A3，`aiPlat-platform/tenants/tenant_store.py`） | ✅ 已覆盖 |
| **前端规范（TS/React）** | **规范-前端架构与工程.md**（P1-B4，approved 2026-08-18）；325 文件有约束 + guard §43-47 + tsc 0 错误 | ✅ 已覆盖 |
| **性能规范** | **规范-性能.md**（P2-B3，approved 2026-08-18） | ✅ 已覆盖 |
| **命名规范** | **规范-命名.md**（P2-B3，approved 2026-08-18） | ✅ 已覆盖 |
| **错误处理规范** | **规范-错误处理.md**（P2-B3，approved 2026-08-18）；历史 496 处 `except Exception: pass` 已于 2026-07-29 全量治理 | ✅ 已覆盖 |
| **独立多租户规范** | **规范-独立多租户.md**（P2-B3，approved 2026-08-18） | ✅ 已覆盖 |
| **日志规范** | 仍无专门规范（仅 §5.20 可观测性顺带 + trace_id 贯穿规范） | ⚠️ 残留（非 P0/P1 阻断项） |

---

## 4. 够用性评估（针对 aiPlat 系统）

**规范"量"足够（300+ 条），且 2026-08-19 复核时"对不上系统的实际风险"问题已全部闭环**：

| 系统实际风险（已知） | 有无规范覆盖 | 结论（2026-08-19） |
|---|---|---|
| 守卫 12 条规则语法 bug（治理体系自身缺陷） | ✅ 已修复 + 新增自检 | **已覆盖**——93b7c25c 修复；P0-C7 黄金样本验证接入 CI（`rule_golden_sample.py --verify`），守卫规则必须有真实命中 |
| 能力登记双源漂移（registry 807 vs 文档 944） | ⚠️ sync_registry_to_docs.py 已建 | **部分覆盖**（P0-C3 部分）：`registry→docs` 漂移未自动清零；当前 registry total=1032 vs 文档 1039（口径不同：registry 登记 vs 扫描） |
| 前端 326 文件无约束 | ✅ 前端规范已定稿 | **已覆盖**——规范-前端架构与工程（approved）+ guard §43-47 + tsc 0 错误（325 文件） |
| 24 项宪法违规（内核无关/职责归属被违反） | ✅ 已清零 | **已闭环**——宪法测试 143 passed + 1 skipped 全绿；守卫 0 ERROR |
| 多租户/计费/配额 | ✅ 规范-独立多租户（approved） | **已覆盖**——隔离/配额/套餐有统一标准；tenant 表迁移 P0-A3 落地 |
| 引擎层硬编码中文 prompt（历史） | ✅ 有（§5.29 内核无关）已修复 | 已覆盖 |

**核心判断（2026-08-19 更新）**：aiPlat 的规范体系**"内核过度规范"的短板已被补齐**——引擎/架构/安全规范依然细（105 条），数据/前端/性能/错误处理/多租户/命名六大外围维度已全部有 approved 规范，且**规范治理了治理体系自身**（守卫黄金样本 + 宪法测试入 CI + 12 条 bug 修复）。剩余残留：①日志规范无专门文档；②registry→CAPABILITIES 漂移未自动清零（需跑 sync_registry_to_docs.py 或接入自动同步）。

---

## 5. 改进建议（按优先级——2026-08-19 复核：原建议全部落地）

> **2026-08-19 更新**：原 §5 的 6 项建议（P0×2 / P1×3 / P2×1）已**全部落地**，下表标注落地证据；仅保留两项非阻断残留。

| 优先级 | 项目 | 落地状态（2026-08-19） | 证据 |
|---|---|---|---|
| **P0** | standards/ 3 份规范从 draft 定稿（消歧：mTLS vs API key、request_id≠run_id、request_dedup） | ✅ **已完成（P0-C1/C2）** | 9/9 approved（3 份 2026-08-16 + 6 份 2026-08-18）；mTLS 消歧见鉴权规范 §1.2；request_dedup 已实现（runs_mixin.py） |
| **P0** | 新增"规范自检"条款：守卫规则配黄金样本测试（防 12 条 bug 重演） | ✅ **已完成（P0-C7）** | `rule_golden_sample.py --verify` 接入 architecture-guard.yml CI；12 条 bug 已修（93b7c25c） |
| **P1** | 新增"单一真相源"规范：能力登记由生成器产出（消除漂移） | ⚠️ **部分完成（P0-C3）** | `sync_registry_to_docs.py` 已建；registry 1032 vs 文档 1039（口径不同），漂移未自动清零——残留项 |
| **P1** | 新增前端规范（TS 架构/API 调用/状态管理） | ✅ **已完成（P1-B4）** | 规范-前端架构与工程（approved）；325 文件有约束 + guard §43-47 + tsc 0 错误 |
| **P1** | 新增数据/DB 规范（schema 归属/迁移/命名）——tenant_quotas 已在核心违规 | ✅ **已完成（P1-B5 + P0-A3）** | 规范-数据与数据库（approved）；tenant_quotas/usage_ledger/policies 迁 platform TenantStore |
| **P2** | 新增多租户/计费/性能/错误处理规范 | ✅ **已完成（P2-B3）** | 规范-独立多租户/性能/错误处理（+命名）均 approved 2026-08-18 |

**残留项（非阻断）**：
1. **日志规范**：仍无专门 standards 文档（日志格式/级别/脱敏无统一标准）
2. **registry→CAPABILITIES 漂移未自动清零**：需将 `sync_registry_to_docs.py` 接入自动同步（当前手动）

---

## 6. 结论

> **2026-08-19 状态更新**：原结论"内核很强、外围很弱、执行断裂"已不成立。复核后：

**aiPlat 的规范体系"内核强、外围已补齐、执行已闭环"**：

- ✅ **内核规范是行业顶级**：105 条 engine 规范 + 190 守卫规则，覆盖架构/依赖/配置驱动/内核无关/记忆/知识/Agent/Skill/安全/模型
- ✅ **正确性已定稿**：standards **9/9 approved**（P0-C1）；mTLS vs API key 已消歧（以代码为准，mTLS 远期）、request_id/run_id 命名空间消歧、request_dedup 已实现（P0-C2）
- ✅ **完整性与够用性已补齐**：数据/DB、前端、性能、命名、错误处理、独立多租户**六大维度全部有 approved 规范**（P1-B4/B5 + P2-B3）；前端 325 文件有约束 + guard §43-47 + tsc 0 错误
- ✅ **执行链路已闭环**：24 项宪法违规清零、宪法测试 143 passed + 1 skipped 全绿、守卫 0 ERROR、治理体系自身有黄金样本自检（P0-C7）
- ⚠️ **残留 2 项（非阻断）**：日志规范无专门文档；registry→CAPABILITIES 漂移未自动清零（sync_registry_to_docs.py 已建，需接入自动同步）

**一句话（2026-08-19）**：**针对 aiPlat 系统，规范体系已从"不够"变为"够用且闭环"**——9/9 规范定稿（P0-C1）、矛盾全部消歧（P0-C2）、六大维度补齐（P1-B4/B5 + P2-B3）、执行链路打通（24 项违规清零 + 宪法测试全绿 + 守卫 0 ERROR）。剩余工作仅为两项非阻断收尾（日志规范、漂移自动清零）。

---

## 7. 审计方法局限

1. "规范 vs 代码矛盾"（C1-C3）基于 standards 逐条对照；2026-08-19 复核时以**代码事实为准**（runs_mixin.py/schema.py/authenticator 交叉验证），未系统扫描全部 CLAUDE.md 条款 vs 代码
2. 维度盲区判定基于 standards/ 目录存在性 + frontmatter status 验证（9/9 approved 已逐一 grep 确认）+ CLAUDE 章节 grep
3. 105 条 core CLAUDE 规范未逐条验证执行状态（2026-08-19 复核依赖宪法测试 143 passed 全绿 + 守卫 0 ERROR 作为整体证据，未逐条复查）
4. "300+ 条规范"为估算（含重复条款），实际独立规范数可能略低
5. 2026-08-19 复核为状态更新复核，未重跑 `architecture_guard.sh` 与 `pytest tests/constitution/`（基线已给出全绿结果，本报告引用之）
